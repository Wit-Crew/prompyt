Create a full-stack AI-powered URL Legitimacy Detection project.

PROJECT GOAL:
Build a web application that analyzes a given URL and determines whether it is Legitimate or Suspicious/Phishing based on example logic patterns and AI analysis.

TECH STACK:
Frontend: React (with modern UI)
Backend: Node.js + Express
AI Integration: OpenAI API (or any AI API)
Database: MongoDB (optional, for logging URL checks)
Styling: Clean modern UI with Tailwind CSS

CORE FEATURES:
1. User enters a URL in an input field.
2. Backend analyzes URL using:
   - Rule-based logic:
        - Check HTTPS
        - Check domain age (mock if needed)
        - Check suspicious keywords (login, verify, free, win, etc.)
        - Check IP-based URLs
        - Check long/random subdomains
   - AI-based analysis using OpenAI API:
        - Send URL and extracted features to AI
        - AI responds with:
            {
              verdict: "Legitimate" | "Suspicious",
              confidence: percentage,
              explanation: short reason
            }
3. Display result with:
   - Status badge (Green = Legit, Red = Suspicious)
   - Confidence %
   - Explanation text
4. Store scan history in MongoDB with:
   - URL
   - Verdict
   - Confidence
   - Timestamp

PROJECT STRUCTURE:
- /client (React app)
- /server (Node + Express backend)
- .env file for API keys
- Modular folder structure:
    - controllers
    - services
    - routes
    - utils (URL analysis logic)
    - models (if using MongoDB)

BACKEND REQUIREMENTS:
- POST /analyze endpoint
- Validate input URL
- Extract domain details
- Apply rule-based scoring system
- Call OpenAI API using proper async/await
- Combine rule score + AI response for final verdict
- Proper error handling

FRONTEND REQUIREMENTS:
- Clean UI with:
    - Input field
    - Analyze button
    - Loading spinner
    - Result card
    - Scan history section
- Responsive design
- Proper API integration

EXTRA:
- Add basic rate limiting
- Add input validation
- Add loading state
- Add environment configuration
- Include README with setup instructions

IMPORTANT:
- Use modern JavaScript (ES6+)
- Use async/await
- Use proper project architecture
- Add comments explaining logic
- Provide example OpenAI API call implementation
- Mock AI response if API key not available

Generate complete working code for all files.

using html,css,javascript
